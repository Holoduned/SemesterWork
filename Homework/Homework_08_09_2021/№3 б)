using System;

namespace homework
{
    class program
    {
        static void Main()
        {
            int x = Convert.ToInt32(Console.ReadLine());
            double y = (x + 4) * (1 + (x + 3) * (1 + (x + 2) * (1 + (x + 1))));

            Console.WriteLine(y);
        }
    }
}